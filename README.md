# 🚀 Steam Hours Boost (Multi-Account)

สคริปต์สำหรับดันชั่วโมงเกม Steam (Idle) ที่รองรับการทำงานหลายบัญชีพร้อมกัน มีระบบหน่วงเวลาเพื่อป้องกันการโดนแบน IP และรองรับการกรอกรหัส Steam Guard ผ่านคอนโซล

## ✨ คุณสมบัติ (Features)
* **Multi-Account:** รันหลายบัญชีพร้อมกันได้ไม่จำกัด
* **Smart Delay:** ระบบสุ่มหน่วงเวลาระหว่างเริ่มแต่ละบัญชี (12-20 วินาที) เพื่อความปลอดภัย
* **Steam Guard Support:** รองรับทั้งการใช้ `shared_secret` (สร้างรหัสอัตโนมัติ) หรือกรอกรหัสผ่าน Console ด้วยตัวเอง
* **Auto Reconnect:** เชื่อมต่อใหม่อัตโนมัติเมื่อหลุดจากเซิร์ฟเวอร์ หรือเมื่อเซิร์ฟเวอร์ Steam ปิดปรับปรุง
* **Smart Idle:** หยุดดันชั่วโมงชั่วคราวเมื่อคุณเข้าเล่นเกมเอง และจะเริ่มใหม่โดยอัตโนมัติในอีก 2 ชั่วโมงถัดไป
* **Detailed Logging:** แสดงสถานะการทำงานเป็นภาษาไทย เข้าใจง่าย

---

## 🛠 การติดตั้ง (Installation)

### 1. เตรียมความพร้อม
ติดตั้ง [Node.js](https://nodejs.org/) (แนะนำเวอร์ชัน 18 ขึ้นไป)
ใช้คำสั่งนี้เพื่อเพิ่ม Repository และติดตั้ง Node.js:
# อัปเดตระบบก่อนเริ่ม
```bash
sudo dnf update -y
```
# เพิ่ม NodeSource Repository (สำหรับ Node.js 20)
```bash
curl -sL https://rpm.nodesource.com/setup_20.x | sudo bash -
```
# ติดตั้ง Node.js และเครื่องมือสำหรับ Compile
```bash
sudo dnf install -y nodejs gcc-c++ make
```
# ตรวจสอบเวอร์ชัน
```bash
node -v
npm -v
```
### 2. ดาวน์โหลดและติดตั้ง Library
```bash
git clone [https://github.com/YOUR_USERNAME/steam-hours-boost.git](https://github.com/YOUR_USERNAME/steam-hours-boost.git)
cd steam-hours-boost
npm install
```

## ⚙️ การตั้งค่า (Configuration)
แก้ไขข้อมูลบัญชีในไฟล์ accounts.json:
```bash
[
  {
    "username": "your_username_1",
    "password": "your_password_1",
    "shared_secret": "YOUR_SHARED_SECRET", 
    "games": [730, 440, 570],
    "status": 1
  },
  {
    "username": "your_username_2",
    "password": "your_password_2",
    "shared_secret": "", 
    "games": [730],
    "status": 7
  }
]
```

* shared_secret: ถ้ามีค่า จะสร้างรหัส 2FA อัตโนมัติ หากไม่มีระบบจะถามให้กรอกใน Console
* games: ใส่ AppID ของเกมที่ต้องการดันชั่วโมง (หาได้จาก SteamDB)
* status: 1 (Online), 7 (Invisible)

## วิธีการใช้งาน (Usage)
การรันปกติ (Windows/Linux)
```bash
node index.js
```
การรันบน VPS ให้ทำงานตลอด 24 ชั่วโมง (แนะนำ PM2)
```bash
# ติดตั้ง PM2
npm install pm2 -g

# เริ่มทำงาน
pm2 start index.js --name "steam-idle"

# ดูสถานะและ Log
pm2 status
pm2 logs steam-idle
```
