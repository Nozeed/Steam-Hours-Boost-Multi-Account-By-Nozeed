# 🚀 Steam Hours Boost (Multi-Account) 🎮

สคริปต์สำหรับดันชั่วโมงเกม Steam (Idle farming) ที่รองรับการทำงาน **หลายบัญชีพร้อมกัน** พัฒนาด้วย Node.js  
มีระบบความปลอดภัยและจัดการสถานะการออนไลน์ที่ชาญฉลาด ช่วยลดโอกาสโดน Steam ตรวจจับผิดปกติ

**⚠️ คำเตือนสำคัญ**  
การใช้สคริปต์นี้เพื่อ idle เกมอาจขัดต่อ [Steam Subscriber Agreement](https://store.steampowered.com/subscriber_agreement/) ในบางกรณี ใช้ด้วยความระมัดระวังและรับผิดชอบเอง

---

## ✨ คุณสมบัติเด่น

- 👥 **Multi-Account** — รันหลายบัญชีพร้อมกันได้ไม่จำกัด (ใน process เดียว)
- 🛡️ **Smart Delay** — สุ่มหน่วงเวลา 12–20 วินาทีระหว่าง login แต่ละบัญชี ป้องกัน rate limit / flag IP
- 🔐 **Steam Guard Support**  
  - ใช้ `shared_secret` → สร้างโค้ด 2FA อัตโนมัติ  
  - ไม่มี shared_secret → ระบบจะถามรหัสผ่านคอนโซล (interactive)
- 🔄 **Auto Reconnect** — เชื่อมต่อใหม่เองเมื่อหลุด / Steam maintenance
- 💤 **Smart Idle** — หยุด idle ชั่วคราวเมื่อคุณเล่นเกมจริง แล้วเริ่มใหม่เองอัตโนมัติ
- 🇹🇭 **Thai Language Log** — แสดงสถานะภาษาไทยชัดเจน บอกสาเหตุ error ทุกกรณี

---

## 🛠️ การติดตั้ง

### Prerequisites
- Node.js v18+ (แนะนำ LTS v20 หรือ v22)
- git

### บน Ubuntu / Debian (20.04, 22.04, 24.04)

```bash
# 1. อัพเดตระบบ
sudo apt update && sudo apt upgrade -y

# 2. ติดตั้ง Node.js LTS (จาก nodesource – แนะนำวิธีนี้)
curl -fsSL https://deb.nodesource.com/setup_lts.x | sudo -E bash -
sudo apt install -y nodejs

# เช็คเวอร์ชัน (ควรได้ ~v20.x หรือสูงกว่า)
node -v
npm -v

# 3. ติดตั้ง git
sudo apt install -y git
git clone https://github.com/Nozeed/Steam-Hours-Boost-Multi-Account-By-Nozeed.git
cd Steam-Hours-Boost-Multi-Account-By-Nozeed
```

### ติดตั้ง Dependencies
```bash
npm install
# หรือเฉพาะ package ที่จำเป็น (ตาม package.json เดิม)
npm install steam-user steam-totp
```

### ⚙️ การตั้งค่า (Configuration)
แก้ไขข้อมูลในไฟล์ accounts.json ใช้ VS Code / notepad ถ้าใช้บน Windows :
```bash
[
  {
    "username": "ไอดี_steam_1",
    "password": "รหัสผ่าน_1",
    "shared_secret": "รหัส_2FA_ถ้ามี",
    "games": [730, 440, 570],
    "status": 1
  },
  {
    "username": "ไอดี_steam_2",
    "password": "รหัสผ่าน_2",
    "shared_secret": "",
    "games": [730],
    "status": 1
  }
]
```
* shared_secret: ถ้าเว้นว่างไว้ ระบบจะขอให้กรอกรหัส Steam Guard ในหน้าจอดำตอนเริ่มทำงาน
* games: ใส่ AppID ของเกมที่ต้องการ (หาได้จาก SteamDB)
* status: 1 = Online, 7 = Invisible (ซ่อนสถานะ)

แก้ไขข้อมูลในไฟล์ accounts.json (แนะนำให้ใช้ vi หรือ nano ถ้าใช้บน Linux):
```bash
nano accounts.json
```
(กด Ctrl+O แล้ว Enter เพื่อบันทึก และ Ctrl+X เพื่อออก)

---

### วิธีการใช้งาน (Usage)

**1 รันปกติ (สำหรับทดสอบ)**
```bash
node index.js
```

**2 การรันบน VPS (24 ชั่วโมง) ด้วย PM2**

```bash
# ติดตั้ง PM2 (global)
sudo npm install -g pm2

# รันสคริปต์
pm2 start index.js --name "steam-boost"

# ดู log (สำคัญมาก ถ้ามี 2FA จะให้กรอกที่นี่)
pm2 logs steam-boost

# ดูสถานะ
pm2 status

# ทำให้รันอัตโนมัติตอนเครื่อง reboot
pm2 startup
pm2 save

# คำสั่งอื่นที่ใช้บ่อย
pm2 restart steam-boost
pm2 stop steam-boost
pm2 delete steam-boost
```
